\# TOOLING



Primary tools:

\- Docker MCP Gateway (MCP servers: github, fetch, playwright, filesystem, stripe, etc.)



Process:

\- Admin runs Resource Intake before work

\- If missing secrets: show USER TODO LIST

\- Work happens on branches

\- QA + Security gates must pass

\- Merge only when GitHub checks are green

\- Update PROJECT\_STATE + CHECKLIST after finishing

